{
  "name": "graphics-saver",
  "version": "1.1.0",
  "private": true,
  "description": "Универсальное браузерное расширение для сохранения изображений с любых сайтов.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:watch": "node --test --watch tests/*.test.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
